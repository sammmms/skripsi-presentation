/** Single import site for framer-motion's reduced-motion hook, so the rest of
 *  the app depends on `@/hooks/useReducedMotion` instead of the library path. */
export { useReducedMotion } from 'framer-motion'
